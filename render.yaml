services:
  - type: worker
    name: jallal-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_render.py
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
